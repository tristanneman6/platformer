sprite[RIGHT] = spr_player_right;
sprite[LEFT] = spr_player_left;

face = RIGHT;

hsp = 0; // Horizontal speed
vsp = 0; // Vertical speed
grv = 0.5; // Gravity
jump_speed = -5.5; // Jump speed

// Control variables
move_speed = 2.5; // Movement speed
on_ground = false; // Jump state